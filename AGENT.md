# Repository publishing instructions

## “发布”触发规则

当用户在本仓库任务中明确说“发布”时，视为已授权执行以下完整发布流程，不需要再次询问是否提交或推送：

1. 检查当前分支、工作区状态和待提交差异。
2. 运行与改动相匹配的校验；至少运行 `npm run build`。涉及交互行为时，同时运行相应端到端测试。
3. 校验通过后，将本次工作区内的全部改动提交到本地 `main` 分支：
   - 使用能准确概括本次改动的提交信息。
   - 如果没有待提交改动，则跳过创建空提交。
   - 如果当前不在 `main`，应以不丢失现有改动的方式安全落到 `main`；出现冲突时停止并报告，不得擅自覆盖。
4. 将本地 `main` 推送到 `origin/main`。
   - 已确认的远端为 `git@github.com:height/fund-exam.git`。
5. 最后报告校验结果、提交 SHA 和推送结果。

## 安全约束

- 禁止 force push。
- 禁止使用 `git reset --hard`、`git checkout --` 等方式丢弃改动。
- 校验失败、发生合并冲突或远端拒绝推送时，不得绕过；停止发布并明确报告原因。
- “提交”只表示创建本地提交；只有用户明确说“发布”时才自动推送远端。
